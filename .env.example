NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# -- Question-package asset generation (scripts/build-question-package.ts) --------------------
# Only needed when generating a package with the external RASTER image provider
# (`--provider external`). The deterministic diagram provider (`--provider diagram`, the default)
# needs none of these. Never commit real keys — these are placeholder names only.
IMAGE_GENERATION_PROVIDER=
IMAGE_GENERATION_API_URL=
IMAGE_GENERATION_API_KEY=
